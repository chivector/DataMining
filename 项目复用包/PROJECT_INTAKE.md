# 项目信息填写表：已有 Prompt 的 API 实验采集

请用户在开始前填写。未填写项标记为“待确认”，不要让 Codex 猜。

## 1. 基本信息

- 项目名称：
- 项目类型：已有 Prompt 的多模型 API 实验采集
- 项目根目录：
- 允许修改目录：
- 禁止修改目录或文件：
- 是否允许运行测试：
- 是否允许运行真实 API：
- 是否允许长时间运行：
- 是否允许多线程并发：

## 2. Prompt 输入

- baseline Prompt 文件：
- 需要纳入的策略 Prompt 文件：
- 是否已有统一 Prompt 表：
- Prompt 必备字段：
- 是否需要新增 `strategy`：
- 是否需要新增 `prompt_uid`：
- 预计 Prompt 总数：

## 3. API 与模型

- API 类型：DF/OpenAI-compatible / OpenAI / Anthropic / DeepSeek / 其他
- API base URL：
- key 提供方式：环境变量 / `.env` / 其他
- 模型列表：
- 是否需要调用 `/models` 获取模型：
- 每条 Prompt 重复次数：
- temperature：
- max_tokens：
- timeout_seconds：
- max_retries：

## 4. 采集运行参数

- 并发数 `--workers`：
- 详细日志间隔 `--log-every`：
- 写盘间隔 `--flush-every`：
- 是否使用 `--schedule interleaved`：
- 是否限制本轮调用数 `--limit`：
- 失败策略：skip / abort / 待确认
- 是否允许中断后续跑：

## 5. 输出要求

- 原始回复输出路径：
- 行为特征输出路径：
- 分析输出目录：
- 是否需要覆盖率表：
- 是否需要失败样本表：
- 是否需要中途阶段性汇总：

## 6. 后续分析边界

- 本阶段是否只交付原始回复：
- 是否要构建行为特征：
- 是否要运行初步分析：
- 哪些内容留给后续数据挖掘同学：
- 不希望提前写成结论的内容：

## 7. 安全与隐私

- 是否含 API key：
- 是否含账号或私人邮箱：
- 是否含私人绝对路径：
- 需要脱敏的内容：
- 禁止写入复用包/报告的内容：

## 8. 最终验收标准

- Prompt 表必须通过哪些检查：
- 原始回复表必须通过哪些检查：
- 特征表必须通过哪些检查：
- 必须运行的测试命令：
- 最终回复需要列出的文件：
- 其他验收要求：
