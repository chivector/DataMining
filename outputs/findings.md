# 自动生成的核心规律发现

基于 1200 条模型回复、4 个模型的统计结果，本文件列出了具有显著差异的发现，可直接写入挖掘分析报告。

- 模型 **Claude-Sonnet-sim** 从 L1 自然语言描述切换到 L3 数学建模时，准确率下降 10.0%（76.0% → 66.0%），提示该模型倾向视觉直觉。
- 模型 **GPT-4o-sim** 从 L1 自然语言描述切换到 L3 数学建模时，准确率下降 17.0%（73.0% → 56.0%），提示该模型倾向视觉直觉。
- 模型 **Small-LLM-sim** 从 L1 自然语言描述切换到 L3 数学建模时，准确率下降 8.0%（43.0% → 35.0%），提示该模型倾向视觉直觉。
- 模型 **Claude-Sonnet-sim** 在 L1-L3 描述迁移上的平均一致性仅 61.3%，最低 33.3%（基于 100 个 case×repeat 组合），存在 **描述越数学越糊涂** 的反常现象。
- 模型 **DeepSeek-V3-sim** 在 L1-L3 描述迁移上的平均一致性仅 54.7%，最低 33.3%（基于 100 个 case×repeat 组合），存在 **描述越数学越糊涂** 的反常现象。
- 模型 **GPT-4o-sim** 在 L1-L3 描述迁移上的平均一致性仅 56.7%，最低 33.3%（基于 100 个 case×repeat 组合），存在 **描述越数学越糊涂** 的反常现象。
- 模型 **Small-LLM-sim** 在 L1-L3 描述迁移上的平均一致性仅 60.0%，最低 33.3%（基于 100 个 case×repeat 组合），存在 **描述越数学越糊涂** 的反常现象。
- 在干扰 **ambient_noise** 下，模型 **Claude-Sonnet-sim** 的判断翻转率达到 43.3%（共 60 个样本），说明无关信息显著改变了它的几何推理结论。
- 在干扰 **lighting** 下，模型 **DeepSeek-V3-sim** 的判断翻转率达到 51.7%（共 60 个样本），说明无关信息显著改变了它的几何推理结论。
- 在干扰 **lighting** 下，模型 **GPT-4o-sim** 的判断翻转率达到 48.3%（共 60 个样本），说明无关信息显著改变了它的几何推理结论。
- 在干扰 **lighting** 下，模型 **Small-LLM-sim** 的判断翻转率达到 63.3%（共 60 个样本），说明无关信息显著改变了它的几何推理结论。
- 模型 **GPT-4o-sim** 错误样本中占比最高的失效模式是 `concept_confusion`（66.4%，73 例），建议在报告中重点展开该模式的典型回复。
- 模型 **Claude-Sonnet-sim** 错误样本中占比最高的失效模式是 `concept_confusion`（60.9%，53 例），建议在报告中重点展开该模式的典型回复。
- 模型 **DeepSeek-V3-sim** 错误样本中占比最高的失效模式是 `concept_confusion`（58.9%，63 例），建议在报告中重点展开该模式的典型回复。
- 模型 **Small-LLM-sim** 错误样本中占比最高的失效模式是 `concept_confusion`（57.1%，105 例），建议在报告中重点展开该模式的典型回复。
- 当模型在回复中复述触发词 **催促** 时，整体准确率 0.0% 低于全局均值 59.3%（差 59.3%，命中样本 51 例），提示该词容易把模型从几何推理拉向感性判断。
- 当模型在回复中复述触发词 **噪声** 时，整体准确率 0.0% 低于全局均值 59.3%（差 59.3%，命中样本 33 例），提示该词容易把模型从几何推理拉向感性判断。
- 当模型在回复中复述触发词 **扶手** 时，整体准确率 0.0% 低于全局均值 59.3%（差 59.3%，命中样本 45 例），提示该词容易把模型从几何推理拉向感性判断。
